# {{ ansible_managed }}

# loopback
127.0.0.1	localhost
::1 localhost ip6-localhost ip6-loopback
ff02::1 ip6-allnodes
ff02::2 ip6-allrouters

# Rheinufer hosts
{% for host in groups['rheinufer'] %}
{{ hostvars[host].ipv4.address }} {{ host }} {{ hostvars[host].cname }}
{{ hostvars[host].ipv6.address }} {{ host }} {{ hostvars[host].cname }}
{% endfor %}
